// Package observability exposes HTTP handlers and helpers for runtime visibility:
// JSON container stats from the Docker API ([Collector], [StatsHandler]) and
// WebSocket streaming of container logs ([LogsStreamer]).
package observability
